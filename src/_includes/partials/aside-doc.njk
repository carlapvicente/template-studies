<aside class="portal__aside" aria-label="Navegação da documentação">
  {% if asideTopButton %}
  <div class="aside__top-button">
    {% set href = asideTopButton.href %}
    {% set text = asideTopButton.text %}
    {% set variant = asideTopButton.variant or 'nav' %}
    {% set icon = asideTopButton.icon or '' %}
    {% include "partials/button.njk" %}
  </div>
  {% endif %}

  <nav class="portal__nav" aria-label="Documentação">
    <h2 class="course__doc-title">Documentação</h2>
    <ul class="portal__nav-list">
      <li><a href="{{ '/' | url }}" class="portal__nav-link">Início</a></li>
      
      {# Botão de Roadmap: Aparece em setup.njk e glossary.njk #}
      {% if page.filePathStem != '/docs/roadmap' %}
      <li><a href="{{ '/docs/roadmap/' | url }}" class="portal__nav-link">Roadmap do curso</a></li>
      {% endif %}
      
      {# Botão de Setup: Aparece em roadmap.njk e glossary.njk #}
      {% if page.filePathStem != '/docs/setup' %}
      <li><a href="{{ '/docs/setup/' | url }}" class="portal__nav-link">Setup do ambiente</a></li>
      {% endif %}
      
      {# Botão de Glossário: Aparece em roadmap.njk e setup.njk #}
      {% if page.filePathStem != '/docs/glossary' %}
      <li><a href="{{ '/docs/glossary/' | url }}" class="portal__nav-link">Glossário</a></li>
      {% endif %}
    </ul>
  </nav>

  <section class="doc-info">
    <h2 class="doc-info__title">Dica Rápida</h2>
    <p class="doc-info__text">
      Complete cada módulo de forma progressiva. Não pule níveis! 🚀
    </p>
  </section>
</aside>