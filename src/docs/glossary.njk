---
layout: layouts/base.njk
pageTitle: Glossário
title: Glossário
subtitle: Definições claras para termos essenciais encontrados no curso.
headerType: doc
asideType: doc
permalink: /docs/glossary/
---
{% import "macros/ui.njk" as ui %}

<section class="portal__section">
  <h1 class="portal__section-title"><i class="fa-solid fa-book-atlas"></i> Termos Técnicos</h1>
  <p class="portal__introduction-text">
    Este glossário é gerado automaticamente a partir dos tooltips encontrados nos módulos do curso.
  </p>

  <hr class="portal__divider">

  {% if glossary and glossary.length > 0 %}
    <div class="glossary-list">
      {% for item in glossary %}
      <div class="glossary-item" id="term-{{ item.term | slugify }}">
        <h3 class="glossary-term">
          {{ item.term }}
          <a href="#term-{{ item.term | slugify }}" class="glossary-anchor" aria-label="Link para este termo">#</a>
        </h3>
        <p class="glossary-definition">{{ item.definition | safe }}</p>
      </div>
      {% endfor %}
    </div>
  {% else %}
    {{ ui.alert(
      type='information',
      title='Glossário Vazio',
      text='Ainda não há termos definidos nos módulos. Adicione tooltips nas aulas para popular esta lista.'
    ) }}
  {% endif %}

</section>